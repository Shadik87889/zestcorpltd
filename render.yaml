services:
  - type: web
    name: my-ielts-site # You can name this whatever you like
    env: static
    staticPublishPath: . # Serve files from the root directory
    rewrites:
      - source: "/:path*"
        destination: "/:path*.html"
